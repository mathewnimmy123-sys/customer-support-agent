# cloudbuild.yaml
steps:
  - name: 'python:3.11-slim'
    id: 'deploy-to-agent-engine'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e

        echo "==> Step 1: Upgrade base installer tools"
        python3 -m pip install -q --no-cache-dir --upgrade pip setuptools wheel

        echo "==> Step 2: Install baseline deploy dependencies"
        python3 -m pip install -q --no-cache-dir \
          "packaging==24.2" \
          "google-cloud-aiplatform[reasoningengine]==1.71.1"

        echo "==> Step 3: Running deploy.py"
        python3 deploy.py

timeout: '1200s'
options:
  logging: CLOUD_LOGGING_ONLY